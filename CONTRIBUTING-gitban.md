`
   comments like this one (they vanish on render), NEVER in HTML `<!-- ... -->`
   comments (which DO render and would trip the citation scan).
#}
<!-- Generated from .gitban/forge.yaml by gitban. Edit that file, not this one — gitban update overwrites this doc. -->

# Contributing to this repository (gitban fork workflow)

This repository uses [gitban](https://github.com/muunkky/gitban-mcp) to coordinate
a fork-based contribution workflow. This document is **generated** from
`.gitban/forge.yaml` — to change it, edit that file and re-run `gitban update`.

## Remotes

This repo's collaboration topology declares the following git remotes. The
**side** is security-critical: gitban may auto-publish to a `fork`, but never
auto-writes to an `upstream`.

| Remote | Repository | Side |
| :--- | :--- | :--- |
| `origin` | `muunkky/our-fork` | fork |
| `upstream` | `octo/canonical` | upstream |

Provider: **github** (topology version `1`).

## Overlay policy

gitban overlays its working state onto this repo. The fork **tracks** these paths
on purpose, and **ignores** the regenerable runtime paths (added to `.gitignore`
on `gitban update --contributing`).

**Tracked (committed to the fork):**

- `.gitban/`
- `.claude/`


**Ignored (regenerable, gitignored):**

- `.gitban/worktrees/`
- `.gitban/logs/`
- `.gitban/traces/`
- `.gitban/audit/`
- `.gitban/views/`



## Branch model

| Role | Branch pattern |
| :--- | :--- |
| monolith | `sprint/{tag}` |
| process_diff | `sprint-record/{tag}` |
| contribution | `contrib/{slug}` |



## Conventions

How this project handles issues, pull requests, and discussion threads:

- **Issues:** sprint tracking is on; unblock requests use the prefix `[unblock]`.

- **Pull requests:** draft-first; gitban offers PRs rather than pushing them.

- **Discussions:** narrative-thread style.


---

*This file is regenerated by `gitban update`. Hand edits are overwritten. The
source of truth is `.gitban/forge.yaml`.*
